import { test, expect } from '@specter/test';

test.describe.configure({ mode: 'serial' })

test.describe('HUD Tests', () => {

    test('HUD shows correct initial score', async ({ world }) => {
        // TODO: Implement test to verify the initial score starts at 0 for players
        // and that the HUD elements are visible on screen.
    });

});
